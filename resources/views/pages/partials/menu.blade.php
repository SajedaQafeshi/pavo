<div class="nav-scroller py-1 mb-2 container">
			<nav class="nav d-flex justify-content-between">
				<a class="p-2 text-muted lang" key="home" href="{{url('home')}}">
				{{ __('main.home') }}
				</a>
				<a class="p-2 text-muted lang" key="catalog" href="{{url('catalog')}}">
				{{ __('main.catalog') }}
					
				</a>
				<a class="p-2 text-muted lang" key="jacket" href="#">
				{{ __('main.jacket') }}
				</a>
				<a class="p-2 text-muted lang" key="about" href="#about">
				{{ __('main.about') }}
				</a>
				<a class="p-2 text-muted lang" key="home" href="#">
				{{ __('main.new') }}
					
				</a>

			</nav>
		</div>